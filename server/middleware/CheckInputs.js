class CheckInputs {
//   static checkCreateInputs(req, res, next) {
//     const {
//       username, password, firstname, lastname, email, cell
//     } = req.body;
//     if (username && password && firstname && lastname && email && cell === undefined) {
//       return res.status(400).send({ status: 'failed', message: 'All values must be entered' });
//     }
//     next();
//   }

//   static checkUpdateInputs(req, res, next) {
//     const {
//       username, password, firstname, lastname, email, cell
//     } = req.body;
//     if (username || password || firstname || lastname || email || cell === undefined) {
//       return res.status(400).send({ status: 'failed', message: 'One record must be updated' });
//     }
//     next();
//   }
}
